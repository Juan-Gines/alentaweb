export class Section {  
  title;
  url;
  icon;
  description;

  constructor(title, url, icon, description){
    this.title = title
    this.url = url
    this.icon = icon
    this.description = description
  }
}